[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cabbage"
version = "0.1.0"
description = "Tiny CLI for quick LLM queries from the terminal"
requires-python = ">=3.8"

[project.scripts]
cabbage = "cabbage.cli:main"
